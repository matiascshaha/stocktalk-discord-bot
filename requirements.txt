discord.py-self==2.1.0
openai>=1.0.0
# Optional providers (install only if you use them):
# anthropic==0.39.0
# google-generativeai>=0.3.0
pyyaml==6.0.1
pydantic==2.12.5

# Webull OpenAPI SDK (official)
webull-openapi-python-sdk==1.0.9

# Additional dependencies
python-dotenv>=1.0.0  # For .env file support
requests>=2.31.0      # HTTP library (used by SDK)
yfinance>=0.2.66,<1.0 # Free Yahoo data source for quote/options probe (kept <1.0 to avoid curl_cffi conflicts)

# Optional: For testing
pytest==7.4.3         # Testing framework
pytest-mock>=3.11.1   # Mocking for tests
pytest-asyncio==0.21.1
pytz==2025.2
