name: Agent CI

on:
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validation placeholder
        run: |
          echo "Replace this step with your real lint/typecheck/test commands"
          echo "Or call scripts/validate.ps1 in a PowerShell job"
